# NTT Startup Challenge CTA Copy Update

Date: 2026-04-18
Workspace: `/Users/mm/Documents/landing-nttstartupchalenge`
Scope: CTA copy and button layout refinement on the landing page

## Goal

Replace the current CTA labels with clearer, more descriptive copy while preserving a polished layout across the header, hero, and closing CTA section.

## Approved Copy

- Replace `Info Event` with `Learn More About NTT Startup Challenge 2026`
- Replace `Partner With Us` with `Startup Registration for NTT Startup Challenge`

These labels should be used consistently anywhere the existing CTA pair appears on the homepage.

## Recommended Approach

Use the approved copy everywhere and refine the shared CTA classes so longer text remains readable and visually balanced.

Why this approach:

- keeps the page copy consistent across all CTA placements
- avoids one-off layout fixes for each section
- supports both desktop and mobile without introducing alternate button text

## Layout Rules

- CTA text may wrap to two lines when needed
- Button labels should stay center-aligned
- Buttons should use a minimum height instead of a fixed height so wrapped text does not look cramped
- Buttons in stacked mobile layouts should fill available width cleanly
- Buttons in row layouts should keep balanced widths and spacing without overflowing the container

## Affected Areas

- Header navigation CTA
- Hero CTA group
- Closing CTA group

## Non-Goals

- No changes to CTA destinations
- No section reordering
- No changes to the countdown, about copy, or overall page structure

## Validation

- Confirm all CTA instances use the approved text
- Check that long labels remain readable on narrow screens
- Check that desktop CTA groups still look balanced and intentional
